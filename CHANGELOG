# Releases

## v0.3.0

* use block length if null salt is provided instead of hash out length (makes not difference, hmac always pads to block length) #1
* add OSWAP dependency check plugin to Maven POM #2

## v0.2.0

* add various unit tests
* update maven plugins

## v0.1.0

initial version
